# https://taskfile.dev
---
version: "3"

tasks:
  run:
    cmds:
      - go run .
    silent: true

  format:
    cmds:
      - go fmt ./...
  lint:
    cmds:
      - go vet ./...
  test:
    cmds:
      - go test ./...

  build:
    cmds:
      - go build .
    deps:
      - download-assets
  download-assets:
    cmds:
      - ./assets/download.sh
    sources:
      - assets/download.sh
    generates:
      - assets/css/pico.css
